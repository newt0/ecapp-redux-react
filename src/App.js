import "./App.css";
import { Router } from "react-router";

const App = () => {
  return (
    <main>
      <Router />
    </main>
  );
};

export default App;

// error: import { Router } from "react-router":にしていた
